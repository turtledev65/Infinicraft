import type { Config } from "./types";

const config: Readonly<Config> = {
    startingItems: [
        { emoji: "💀", name: "skull" },
        { emoji: "💧", name: "water" },
    ],
}
export default config;
